# Secrets / local environment — NEVER commit (use .env.example as the template)
.env
**/.env
!.env.example
!**/.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Logs / runtime output
*.log
*.out

# Local data dumps
workers/data/

# Laravel storage symlink (regenerated via `php artisan storage:link`)
backend/public/storage

# Node
node_modules/

# PHP dependencies
vendor/

# OS / editor
.DS_Store

# Generated/seeded language data (run workers/tools/seed_tedim_hymns.py at deploy)
workers/data/hymns_td.json
workers/tools/.labusaal-cache/
