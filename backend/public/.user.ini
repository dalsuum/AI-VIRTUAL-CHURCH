; Per-directory PHP overrides for the Laravel public entry point (PHP-FPM reads
; .user.ini for PHP_INI_PERDIR settings). Raise the upload ceiling so admins can
; upload Bible background-music tracks; app-side validation still caps at 10 MB.
upload_max_filesize = 15M
post_max_size = 16M
